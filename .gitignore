# --- Python -------------------------------------------------------------------
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# --- Third-party checkouts (cloned/built during setup, not our code) ----------
# Local Pokemon Showdown server (see README "Setup")
server/
# Foul Play benchmark opponent
foul-play/
# Phase-2 poke-engine fork: a local Rust build, not a submodule
poke-engine-fork/

# --- Generated artifacts ------------------------------------------------------
# Value-net training data (regenerated by gen_value_data.py)
data_value*/
# Saved battle replays + decision traces (ladder.py / bench_foulplay.py)
replays/
# TensorBoard logs
tb_logs/
# Legacy PPO checkpoints: large, regenerable from legacy/train_*.py
legacy/old_models/
*.zip
*.log

# --- Scratch ------------------------------------------------------------------
backup_old/

# --- Local / secret -----------------------------------------------------------
# Showdown ladder credentials (never commit)
.env
# Claude Code local settings (personal, machine-specific)
.claude/settings.local.json
